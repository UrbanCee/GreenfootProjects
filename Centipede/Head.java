import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class Head here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Head extends Body
{
    double dYDist=0.0;
    double dDir=1.0;

    public void act() 
    {
        if (dYDist<=0.0 && (x>getWorld().getWidth()-10 || )
        {
            dYDist=10.0;
            
        }
    }    
}
